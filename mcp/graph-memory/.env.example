# Graph Memory MCP configuration
# DB_PATH defaults to <script-dir>/../data/graph.db if not set.
# Override with an absolute or relative (to cwd) path:
# DB_PATH=/absolute/path/to/graph.db
LOG_LEVEL=info
VIEWER_PORT=4000
